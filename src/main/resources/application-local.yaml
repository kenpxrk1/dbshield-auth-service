server:
  port: 8880

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/dbshield
    username: dbshield
    password: dbshield
    hikari:
      schema: auth_service

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_schema: auth_service
    open-in-view: false

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
    default-schema: auth_service
    enabled: true

jwt:
  secret-key: co7IB8BVZ2ZCWoyjnKbrX4ByykVOvY38vtv2DNK9kN4=
  expire-time-access-token: 900000
  expire-time-refresh-token: 3

scheduling:
  fixed-rate: 900000
